# Plugin Info
version: 0.0.1
title: Node.js
description: Log Parser for Node.js using Winston logging library
min_stanza_version: 1.1.7
supported_platforms:
  - linux
  - windows
  - kubernetes
  - macos
parameters:
  - name: source
    label: Log source
    description: Use this field to specify where your logs are coming from. When choosing the 'file' option, the agent reads in logs from the log paths specified below.  When choosing the 'Kubernetes' options, the agent reads logs from /var/log/containers based on the Pod and Container specified b